1. **修改HTML结构**：

   * 将导航项的激活类从`nav-item-active`改为`active`

   * 为图标添加`icon`类

   * 为文字添加`text`类

   * 将中间突出按钮的类从`center-item`改为`center`

   * 更新图标类名（从`fas`改为`fa`或添加`icon`类）

2. **修改CSS样式**：

   * 确保移动端导航的样式与index.html一致

   * 调整激活状态的颜色和样式

   * 更新中间按钮的样式

3. **修改JavaScript交互**：

   * 更新底部导航的激活状态切换逻辑

   * 确保与index.html的交互逻辑一致

4. **添加缺失的Font Awesome链接**：

   * 确保buildservice.html中包含Font Awesome的CSS链接

5. **测试修改后的效果**：

   * 确保移动端底部导航在各种设备

