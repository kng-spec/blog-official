# 引入index.html导航栏和汉堡菜单样式到capcut.html

## 1. 替换CSS样式
- 将index.html中从第154行开始的.navbar相关样式（包括渐变背景、菜单项渐变颜色、悬停效果等）复制到capcut.html中，替换现有的.navbar相关样式
- 将index.html中从第300行开始的.hamburger相关样式复制到capcut.html中，替换现有的.hamburger相关样式
- 将index.html中从第500行开始的响应式设计样式（针对.nav-menu和.hamburger的媒体查询）复制到capcut.html中，替换现有的响应式设计样式

## 2. 补充JavaScript代码
- 检查capcut.html中的JavaScript代码，确保与index.html中的汉堡菜单交互逻辑一致
- 如果有差异，将index.html中的相关JavaScript代码复制到capcut.html中

## 3. 检查并调整
- 检查capcut.html中是否有其他与导航栏相关的样式或脚本，确保它们与新引入的代码兼容
- 调整可能存在的冲突或重复代码

## 4. 测试
- 确保修改后的capcut.html页面在不同设备上显示正常，导航栏和汉堡菜单功能正常