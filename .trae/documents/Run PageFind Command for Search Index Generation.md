1. Run the command `npx pagefind --site post --base-url "/post"` in the post directory to generate search indexes
2. This will scan all HTML files in the post directory and create/ update the PageFind search index files
3. The generated files will be stored in the existing `pagefind` directory within the post folder
4. This enables search functionality for the

