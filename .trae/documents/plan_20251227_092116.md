## 计划：将 index.html 导航栏和汉堡菜单样式引入 sitesbuild.html

### 1. 分析现状
- **index.html**：具有完整的导航栏样式，包括菜单项渐变背景、悬停动画、响应式设计
- **sitesbuild.html**：导航栏结构类似，但样式更简单，缺少渐变效果和复杂动画

### 2. 实施步骤

#### 2.1 替换导航栏 CSS 样式
- **替换范围**：将 sitesbuild.html 中第 25-82 行的导航栏样式替换为 index.html 中第 154-324 行的完整导航栏样式
- **包含内容**：
  - 导航栏基础样式（.navbar, .nav-container）
  - 菜单项渐变背景（每个菜单项不同颜色）
  - 悬停动画效果（缩放、阴影、渐变流动）
  - 汉堡菜单样式（.hamburger, .bar）
  - Pagefind 搜索样式

#### 2.2 更新响应式设计
- **替换范围**：将 sitesbuild.html 中第 331-378 行的响应式导航样式替换为 index.html 中第 501-639 行的完整响应式样式
- **包含内容**：
  - 移动端菜单布局（fixed 定位、垂直排列）
  - 汉堡菜单动画（点击切换 active 类）
  - 移动端底部导航样式

#### 2.3 统一 JavaScript 功能
- **更新内容**：确保 sitesbuild.html 中的汉堡菜单 JavaScript 与 index.html 一致
- **主要功能**：点击汉堡图标切换菜单显示/隐藏，添加动画效果

### 3. 预期效果
- 两个页面的导航栏和汉堡菜单样式完全一致
- 保持原有的 HTML 结构不变
- 响应式设计在所有设备上正常工作
- 菜单项保持相同的渐变颜色和悬停效果

### 4. 注意事项
- 确保替换后的 CSS 样式不会与现有样式冲突
- 验证汉堡菜单的 JavaScript 功能正常
- 检查移动端显示效果是否符合预期